<div class="container-fluid background-main box-shadow-1 spawn-monitor-fill">
  <div class='row'>
    <div class='auth-form-container'>

      <div class='auth-form'>
        <div class='heading-container'>
          <div class='heading'>UNLOCK ACCOUNT</div>
        </div>

        <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
          <%= devise_error_messages! %>

          <div class="field">
            <%= f.label :email %><br />
            <%= f.email_field :email, autofocus: true %>
          </div>

          <div class="actions">
            <%= f.submit "SUBMIT" %>
          </div>
        <% end %>

        <hr>
        <div class='links'>
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>
