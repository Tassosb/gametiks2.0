<!-- <li class="<%=  self_or_other(message) %>">
  <div class="avatar">
    <img src="https://s3.amazonaws.com/gametiks/users/avatars/Gametiks_Gamercard.jpg" />
  </div>
  <div class="chatboxmessagecontent">
    <p><%= message.body %></p>
    <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b at %I:%M%p") %>">
      <%= message_interlocutor(message).name %> • <%= message.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li> -->
